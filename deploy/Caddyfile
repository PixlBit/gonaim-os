# HTTPS تلقائي: Caddy يجلب الشهادة ويجددها بلا تدخل.
{$EHNA_DOMAIN} {
	encode zstd gzip

	# المساحة خاصة: لا فهرسة ولا أرشفة حتى لو تسرّب الرابط
	header {
		X-Robots-Tag "noindex, nofollow, noarchive"
		-Server
	}

	reverse_proxy ehna:8788
}
